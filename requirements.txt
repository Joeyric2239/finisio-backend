# FINISIO CLEANS — Backend Dependencies
# ─────────────────────────────────────
# Zero external dependencies required!
# Runs on plain Python 3.7+ with standard library only.
#
# Built-in modules used:
#   sqlite3   — database
#   http.server — HTTP server
#   json      — JSON parsing / serialisation
#   uuid      — unique ID generation
#   hashlib   — password hashing (sha256)
#   datetime  — timestamps and date arithmetic
#   re        — URL routing (regex)
#   urllib.parse — query string parsing
#   os        — file paths
#   sys       — command-line arguments
#
# ─────────────────────────────────────
# OPTIONAL: upgrade to FastAPI later
# ─────────────────────────────────────
# fastapi>=0.110.0
# uvicorn[standard]>=0.29.0
# sqlalchemy>=2.0.0
# pydantic>=2.0.0
# python-multipart
